/**
 * Version shown in the bottom-right corner.
 *
 * Bump the last number by one with every update that gets deployed, so it is
 * obvious at a glance whether a machine is running the newest build:
 * 1.10 → 1.11 → 1.12 …
 */
export const GAME_VERSION = '1.16';
